## 1. Dockerfile Modification

- [x] 1.1 Insert `debconf-set-selections` commands for Steam license before `apt-get install` in `Dockerfile`.

## 2. Verification

- [x] 2.1 Run `docker compose build` to verify the fix works. (Skipped: Docker not available in environment)
