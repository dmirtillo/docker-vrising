## 1. Implementation

- [x] 1.1 Update `start.sh` to execute `steamcmd` in two phases (update then install).

## 2. Verification

- [x] 2.1 Rebuild the Docker image.
- [x] 2.2 Run the startup test script `scripts/test-startup.sh` to verify the fix.
